// This component is no longer used, as PWA functionality has been removed
// to ensure compatibility with the Electron desktop build.
// It can be safely deleted.
export default function ServiceWorkerRegistration() {
  return null;
}
